/*
package com.shop.entity;


import jakarta.persistence.*;

import java.util.UUID;


@Entity
public class Order {
    @Id
    @GeneratedValue(strategy = GenerationType.SEQUENCE) // спросить
    @Column(name = "id")
    private UUID id;
    @Column(name = "sumPurchase ") // общая сумма покупок
    private double sumPurchase ;
    //Нужен юзер и его нужно связать с заказом. Объеденить юзера с заказом.
    // 1 юзер 1 заказ. Но у юзера может быть несколько заказов?
    //Корзина с перечислением товаров
    //Нужно связать заказ с товарами? Связь? 1 заказ много товаров.

}
*/
